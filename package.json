{
  "name": "messenger",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000",
    "build": "vite build",
    "preview": "vite preview --port 3000",
    "start": "npm run build && npm run preview "
  },
  "devDependencies": {
    "less": "^4.2.0",
    "vite": "^5.3.1"
  },
  "dependencies": {
    "lodash-es": "^4.17.21",
    "vite-plugin-handlebars": "^2.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
